kojihub_pkgs:
  - "koji-{{ koji_version }}"
  - "koji-hub-{{ koji_version }}"
  - "koji-hub-plugins-{{ koji_version }}"
  - "koji-web-{{ koji_version }}"
  - "koji-utils-{{ koji_version }}"
  - git
  - gnupg2
  - krb5-workstation
  - python3-paho-mqtt
  - python3-configparser

psql_client_pkgs:
  - postgresql
  - python3-psycopg2

kojihub_users_sync_pkgs:
  - python3-requests-gssapi
